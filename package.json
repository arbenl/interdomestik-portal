{
  "name": "interdomestik-member-portal",
  "version": "1.0.0",
  "description": "Member portal for Interdomestik organization",
  "main": "index.js",
  "scripts": {
    "build": "echo 'No build step required for vanilla JS'",
    "test": "npm run test:rules",
    "test:rules": "mocha --require ts-node/register test/**/*.test.ts",
    "lint": "echo 'Linting will be implemented'",
    "deploy": "firebase deploy",
    "serve": "firebase serve",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run",
    "e2e:local": "cypress run --config baseUrl=http://localhost:5000"
  },
  "keywords": [
    "member-portal",
    "firebase",
    "interdomestik"
  ],
  "author": "Manus AI",
  "license": "MIT",
  "dependencies": {
    "firebase": "^12.2.1"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "^5.0.0",
    "@types/chai": "^4.3.16",
    "@types/mocha": "^10.0.7",
    "chai": "^5.1.1",
    "cypress": "^13.13.1",
    "firebase-tools": "^14.15.1",
    "mocha": "^10.7.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.2"
  }
}
